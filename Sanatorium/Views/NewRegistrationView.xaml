<local:JedenViewBase x:Class="Sanatorium.Views.NewRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sanatorium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleResources.xaml"/>
    </UserControl.Resources>
    <!--Full tab grid-->
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Style="{StaticResource UpperTitleLabel}" Grid.Row="0" Content="Dodaj nową rejestrację" />
        <!--Personal info grid-->
        <Grid Grid.Row="1" Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition  Height="auto"/>
                <RowDefinition  Height="auto"/>
                <RowDefinition  Height="auto"/>
                <RowDefinition  Height="auto"/>
                <RowDefinition  Height="auto"/>
                <RowDefinition  Height="auto"/>
                <RowDefinition  Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Numer:"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Id_Rejestracji, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Grid.Row="0" Grid.Column="2" Content="Nazwa:"/>
            <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Path=Nazwa, UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Pacjent:"/>
            <ComboBox Grid.Row="1" Grid.Column="1"
                    ItemsSource="{Binding PacjenciComboboxItems}" 
                                        DisplayMemberPath="Value" SelectedValuePath="Key" 
                                        SelectedValue="{Binding Path=Id_Pacjenta, Mode=TwoWay}"/>
            <Label Grid.Row="1" Grid.Column="2" Content="Jednostka:"/>
            <ComboBox Grid.Row="1" Grid.Column="3"
                    ItemsSource="{Binding IdJednostkiComboboxItems}" 
                                        DisplayMemberPath="Value" SelectedValuePath="Key" 
                                        SelectedValue="{Binding Path=Id_Jednostki, Mode=TwoWay}"/>


            <Label Grid.Row="2" Grid.Column="0" Content="Ilosc:"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Path=Ilosc, UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="4" Content="Data rejestracji:" />
            <DatePicker Grid.Column="1" Grid.Row="4" SelectedDate="{Binding Path=Data, UpdateSourceTrigger=PropertyChanged}"/>



        </Grid>

    </Grid>
</local:JedenViewBase>
