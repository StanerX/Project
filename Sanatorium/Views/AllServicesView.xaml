<local:WszyscyViewBase x:Class="Sanatorium.Views.AllServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sanatorium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleResources.xaml"/>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">

        <!--Full tab grid-->
        <Grid Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Style="{StaticResource UpperTitleLabel}" Grid.Row="0" Content="Umówione zabiegi" />
            <!--Personal info grid-->
            <Expander Grid.Row="2" Header="Zabiegi:">
                <DataGrid Grid.Row="1" Background="Tan" AutoGenerateColumns="False" ItemsSource="{Binding List}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID Zabiegu" Binding="{Binding Path=Id_Zabiegu}"/>
                        <DataGridTextColumn Header="Numer" Binding="{Binding Path=Numer}"/>
                        <DataGridTextColumn Header="Nazwa" Binding="{Binding Path=Nazwa}"/>
                        <DataGridTextColumn Header="Doktor" Binding="{Binding Path=DoktorNazwisko}"/>
                        <DataGridTextColumn Header="Ilosc" Binding="{Binding Path=Ilosc}"/>
                        <DataGridTextColumn Header="Cena" Binding="{Binding Path=Cena}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Expander>
        </Grid>
    </ScrollViewer>
</local:WszyscyViewBase>
