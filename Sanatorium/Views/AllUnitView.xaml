<local:WszyscyViewBase x:Class="Sanatorium.Views.AllUnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sanatorium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleResources.xaml"/>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">

        <!--Full tab grid-->
        <Grid Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Style="{StaticResource UpperTitleLabel}" Grid.Row="0" Content="Wszystkie Jednostki" />
            <!--Personal info grid-->
                <Grid>
                    <DataGrid Grid.Row="1" Background="Tan" AutoGenerateColumns="False" ItemsSource="{Binding List}">

                        <DataGrid.Columns>
                        <DataGridTextColumn Header="Nazwa" Binding="{Binding Path=Nazwa}"/>
                        <DataGridTextColumn Header="Kod" Binding="{Binding Path=Kod}"/>
                        <DataGridTextColumn Header="Miejscowosc" Binding="{Binding Path=Miejscowosc}"/>
                        <DataGridTextColumn Header="Lekarz kierujący" Binding="{Binding Path=Lekarzkier}"/>
                        <DataGridTextColumn Header="Przypadlosc" Binding="{Binding Path=Przypadlosc}"/>
                        <DataGridTextColumn Header="NFZ Nazwa" Binding="{Binding Path=NFZNazwa}"/>
                        <DataGridTextColumn Header="NFZ Kod" Binding="{Binding Path=NFZKod}"/>
                        <DataGridTextColumn Header="NFZ Miasto" Binding="{Binding Path=NFZMiasto}"/>
                        <DataGridTextColumn Header="NFZ Wojewodztwo" Binding="{Binding Path=NFZWojewodztwo}"/>



                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>

        </Grid>
    </ScrollViewer>
</local:WszyscyViewBase>
