<local:WszyscyViewBase x:Class="Sanatorium.Views.AllTimeTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sanatorium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleResources.xaml"/>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">

        <!--Full tab grid-->
        <Grid Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Style="{StaticResource UpperTitleLabel}" Grid.Row="0" Content="Terminarz" />
            <!--Personal info grid-->
            <Expander Grid.Row="2" Header="Terminarz Doktora">
                <Grid>
                    <DataGrid Grid.Row="1" Background="Tan" AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding Path=WybranyDyzur, Mode=TwoWay}">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Path=Id_Terminu}"/>
                            <DataGridTextColumn Header="Doktor Imie" Binding="{Binding Path=DoktorImie}"/>
                            <DataGridTextColumn Header="Doktor Stanowisko" Binding="{Binding Path=DoktorStanowisko}"/>
                            <DataGridTextColumn Header="Doktor Nazwisko" Binding="{Binding Path=DoktorNazwisko}"/>
                            <DataGridTextColumn Header="Doktor Specjalizacja" Binding="{Binding Path=DoktorSpecjalizacja}"/>
                            <DataGridTextColumn Header="Data Dyżuru" Binding="{Binding Path=Data}"/>

                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>

            </Expander>


        </Grid>
    </ScrollViewer>
</local:WszyscyViewBase>
