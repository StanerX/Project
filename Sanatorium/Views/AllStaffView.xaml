<local:WszyscyViewBase x:Class="Sanatorium.Views.AllStaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sanatorium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleResources.xaml"/>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">

        <!--Full tab grid-->
        <Grid Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Style="{StaticResource UpperTitleLabel}" Grid.Row="0" Content="Personel" />
            <!--Personal info grid-->
            <Expander Grid.Row="2" Header="Peronel:">
                <DataGrid Grid.Row="1" Background="Tan" AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding Path=WybranyPersonel, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imie" Binding="{Binding Path=Imie}"/>
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding Path=Nazwisko}"/>
                        <DataGridTextColumn Header="Stanowisko" Binding="{Binding Path=Stanowisko}"/>
                        <DataGridTextColumn Header="Specjalonosc" Binding="{Binding Path=Specjalonosc}"/>
                        <DataGridTextColumn Header="Doswiadczenie" Binding="{Binding Path=Doswiadczenie}"/>
                        <DataGridTextColumn Header="Nazwa Usługi" Binding="{Binding Path=NazwaUsługi}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Expander>
        </Grid>
    </ScrollViewer>
</local:WszyscyViewBase>
