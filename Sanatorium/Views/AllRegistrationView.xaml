<local:WszyscyViewBase x:Class="Sanatorium.Views.AllRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sanatorium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleResources.xaml"/>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">

        <!--Full tab grid-->
        <Grid Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Style="{StaticResource UpperTitleLabel}" Grid.Row="0" Content="Wszystkie Rejestracje" />
            <!--Personal info grid-->
            <Expander Grid.Row="2" Header="Rejestracje">
                <Grid>
                    <DataGrid Grid.Row="1" Background="Tan" AutoGenerateColumns="False" ItemsSource="{Binding List}">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nazwa" Binding="{Binding Path=Nazwa}"/>
                            <DataGridTextColumn Header="Ilosc" Binding="{Binding Path=Ilosc}"/>
                            <DataGridTextColumn Header="Data Rejestracji" Binding="{Binding Path=Data}"/>
                            <DataGridTextColumn Header="Imie Pacjenta" Binding="{Binding Path=PacjentNazwisko}"/>
                            <DataGridTextColumn Header="Nazwa Jednostki" Binding="{Binding Path=JednostkaNazwa}"/>

                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>

            </Expander>


        </Grid>
    </ScrollViewer>
</local:WszyscyViewBase>
