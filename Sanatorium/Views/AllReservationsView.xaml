<local:WszyscyViewBase x:Class="Sanatorium.Views.AllReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sanatorium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="StyleResources.xaml"/>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">

        <!--Full tab grid-->
        <Grid Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Style="{StaticResource UpperTitleLabel}" Grid.Row="0" Content="Wszystkie rezerwacje" />
            <!--Personal info grid-->
            <Expander Grid.Row="2" Header="Rezerwacja">
                <Grid>
                    <DataGrid Grid.Row="1" Background="Tan" AutoGenerateColumns="False" ItemsSource="{Binding List}">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Path=Id_Rezerwacji}"/>
                            <DataGridTextColumn Header="Numer" Binding="{Binding Path=Numer}"/>
                            <DataGridTextColumn Header="Data Rezerwacji" Binding="{Binding Path=DataRezerwacji}"/>
                            <DataGridTextColumn Header="Ilość Pokoi " Binding="{Binding Path=Iloscpokoi}"/>
                            <DataGridTextColumn Header="Cena" Binding="{Binding Path=Cena}"/>
                            <DataGridTextColumn Header="Wygody" Binding="{Binding Path=Wygody}"/>
                            <DataGridTextColumn Header="Doktor" Binding="{Binding Path=DoktorNazwisko}"/>



                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>

            </Expander>


        </Grid>
    </ScrollViewer>
</local:WszyscyViewBase>
